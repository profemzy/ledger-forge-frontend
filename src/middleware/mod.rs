pub mod auth;

// Middleware functions available but not currently used (handlers do manual auth checking)
// Uncomment when switching to middleware-based auth:
// pub use auth::{auth_middleware, AuthUser, extract_auth_user};
